name: query_builder
description: Instead of focusing on the details of writing queries, you'll be able to focus on the basic logic of your app. Thus, you will simplify the read and write operations in your database. This will certainly facilitate the development process.
homepage: "https://github.com/emran619/query_builder/blob/main/lib/query_builder.dart"
repository: "https://github.com/emran619/query_builder"
version: 1.0.0


environment:
  sdk: '>=2.19.6 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.2.8+4
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter: